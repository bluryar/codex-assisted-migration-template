[project]
name = "codex-assisted-migration-template"
version = "0.1.0"
description = "Template for governing long-horizon Codex-assisted migration projects."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
